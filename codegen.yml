overwrite: true
schema: ${SCHEMA_PATH}
generates:
  ./gqlTypes.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
  # ./graphql.schema.json:
  #   plugins:
  #     - 'introspection'
  ./fragmentTypes.json:
    plugins:
      - 'fragment-matcher'
