overwrite: true
schema: ${SCHEMA_PATH}
generates:
  ./src/types/gqlTypes.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
    config:
      scalars:
        ISO8601DateTime: string
        ISO8601Date: string
  # ./graphql.schema.json:
  #   plugins:
  #     - 'introspection'
  ./fragmentTypes.json:
    plugins:
      - 'fragment-matcher'
