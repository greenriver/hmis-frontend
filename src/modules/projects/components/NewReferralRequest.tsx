const NewReferralRequest = () => {
  // const { projectId } = useSafeParams() as { projectId: string };
  // TODO: Render EditRecord for creating a ReferralRequest
  // return (
  //   <EditRecord<InventoryFieldsFragment>
  //     FormActionProps={
  //       create ? { submitButtonText: 'Create Inventory' } : undefined
  //     }
  //     onCompleted={onCompleted}
  //     formRole={FormRole.Inventory}
  //     inputVariables={{ projectId }}
  //     record={inventory || undefined}
  //     localConstants={localConstants}
  //     pickListRelationId={projectId}
  //     title={
  //       !create &&
  //       inventory && <ProjectFormTitle title={title} project={project} />
  //     }
  //   />
  // );
  return null;
};
export default NewReferralRequest;
