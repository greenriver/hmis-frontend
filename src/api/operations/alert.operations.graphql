mutation CreateClientAlert($input: ClientAlertInput!) {
  createClientAlert(input: $input) {
    clientAlert {
      ...ClientAlertFields
    }
    errors {
      ...ValidationErrorFields
    }
  }
}
