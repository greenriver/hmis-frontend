query GetUsers(
  $limit: Int = 10
  $offset: Int = 0
  $filters: UserFilterOptions = null
) {
  users(limit: $limit, offset: $offset, filters: $filters) {
    offset
    limit
    nodesCount
    nodes {
      name
      ...UserFieldsForAdmin
    }
  }
}
